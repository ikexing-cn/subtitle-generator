{
  "name": "subtitle-generator",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run ./index.ts",
    "build": "bun build --compile ./index.ts --outfile=out/sgc"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "compressing": "^1.10.0",
    "fast-glob": "^3.3.1"
  }
}
